[nosetests]
with-spec = 1
spec-color = 1
verbosity = 2
with-coverage = 1
cover-erase = 1
cover-package = service
detailed-errors = 1
nocapture = 1

[coverage:run]
branch = True
source = service

[coverage:report]
show_missing = True
# (optional) uncomment to enforce a minimum
# fail_under = 95
